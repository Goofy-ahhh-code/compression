# Simple RLE Compression Algorithm

## Usage:

```bash
# decompress
./compression decompress file.txt

# compress
./compression compress file.txt
```
please don't use this for important files it will severely f*ck it up somehow i just know
